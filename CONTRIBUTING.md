# Contributing

感谢你愿意改进这套玉米植株采集与标注系统。

## 开发环境

1. 安装 `Python 3.9+`、`Node.js 18+`。
2. 在项目根目录运行：

```bash
chmod +x scripts/dev_up.sh
./scripts/dev_up.sh
```

这会自动启动后端和前端开发服务。

## 提交建议

- 提交前先运行前端构建：

```bash
cd frontend
npm run build
```

- 后端修改后建议至少做一轮基础检查：

```bash
python3 -m compileall backend/app
```

- 尽量保持改动聚焦，不把格式化、重命名和功能修改混在同一个提交里。
- 如果改动影响采集流程、标注交互或导出格式，请在 PR 描述中明确说明。

## Bug 报告

请尽量提供以下信息：

- 运行环境：macOS / Windows / Linux，浏览器版本，手机型号
- 复现步骤
- 预期行为与实际行为
- 控制台报错、终端日志、截图或录屏

## 功能贡献

比较欢迎的方向：

- 标注体验优化
- 导出格式扩展
- 采集流程稳定性改进
- 本地部署与启动体验优化

## 数据与隐私

- 不要提交真实采集图片、隐私数据或敏感标注数据。
- 示例数据、截图和演示素材请优先使用脱敏或公开可分享内容。
